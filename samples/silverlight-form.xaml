<UserControl x:Class="SilverlightApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Width="400" Height="300">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="User Registration" 
                   FontSize="24" Margin="10"/>
        
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Username:"/>
            <TextBox x:Name="txtUsername" Width="200" 
                     Text="{Binding Username, Mode=TwoWay}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Margin="10">
            <TextBlock Text="Email:"/>
            <TextBox x:Name="txtEmail" Width="200"
                     Text="{Binding Email, Mode=TwoWay}"/>
        </StackPanel>
        
        <Button Grid.Row="3" Content="Submit" Width="100" Height="30"
                Command="{Binding SubmitCommand}"/>
    </Grid>
</UserControl>
